# Activation plan

1. Inspect live GitHub and local tools/key locations. No .agents directory or AGENTS.md was found in this checkout or its ancestor directories. No prior task directory exists on the resumed branch.
2. Configure android-release with owner review, administrator bypass disabled, and only master/feature/android-account-exporter deployment branches. Reuse any matching existing identity; reject inconsistent records before key creation.
3. Extend the existing initializer only as needed for secure Windows password storage and an actually verified backup. Obtain passwords through local masked UI, never the agent channel. Retain the encrypted PKCS12 outside Git and protect local directories with ACLs. The owner delegated selecting a backup subdirectory; use D:/MementoMori-Signing-Backup and document its same-computer limitation.
4. Run local regression checks and inspect prior exact-HEAD emulator evidence. Fix setup defects, update PR #9, and integrate development dependencies after CI. Prepare a concrete master integration for owner approval; GitHub requires the dispatch workflow on the default branch.
5. Follow the existing exact-source physical-device gate before stable signing. Validate package, version, certificate and hashes. Do not label development/device-test builds as daily packages. Record actual phone acceptance separately from synthetic emulator results.

Passwords remain Windows-DPAPI protected on this computer; a password-protected PKCS12 backup remains portable only with its separately retained password. Do not claim DPAPI alone survives Windows/account loss.
